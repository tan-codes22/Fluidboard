# Public app name shown in the UI and document title.
VITE_APP_NAME=FluidBoard

# Base URL used when generating shareable board links.
# Leave empty to use the current origin at runtime (recommended).
VITE_PUBLIC_BASE_URL=
